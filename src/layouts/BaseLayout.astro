---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const base = import.meta.env.BASE_URL.replace(/\/+$/, ''); // normalized: "" or "/partner-scrape"
const { title, description = 'San Diego STEM Ecosystem — Connecting everyone to STEM learning opportunities.', ogImage = `${base}/images/logo.svg` } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site || 'https://www.sdstemecosystem.org');
// og:image must be an absolute URL for social scrapers; resolve the base-aware
// path against the canonical origin (leaves already-absolute http(s) URLs intact).
const ogImageURL = new URL(ogImage, canonicalURL).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/jpeg" href={`${base}/favicon.jpg`} />
    <link rel="canonical" href={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:type" content="website" />
    <title>{title}</title>
    <!-- Umami analytics (analytics.jtlapp.net); is:inline keeps Astro from bundling it. -->
    <script
      is:inline
      defer
      src="https://analytics.jtlapp.net/script.js"
      data-website-id="949cff30-abab-4247-b13a-d27093fabc4f"
    ></script>
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
